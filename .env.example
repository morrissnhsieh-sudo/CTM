# ─── Vertex AI (primary LLM) ─────────────────────────────────
# Path to your Google Cloud service account JSON key file
VERTEX_KEY_PATH=C:/Users/User/Code/VertexKeys/d-sxd110x-ssd1-aaos-34f80b5f4448.json

# ─── Fallback LLM keys (optional) ────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# ─── Email / Webhook (optional) ──────────────────────────────
RESEND_API_KEY=re_...

# ─── Auto-configured by docker-compose ───────────────────────
# (These are set in docker-compose.yml for local dev)
# DB_URL=postgresql://ctm:ctm_dev_pass@localhost:5432/ctm
# REDIS_URL=redis://localhost:6379
# KAFKA_BROKERS=localhost:9092
# KEYCLOAK_ISSUER=http://localhost:8080/realms/ctm
